import styled from 'styled-components';
import BorderBottom from '../BorderBottom';

export default styled(BorderBottom)`
  bottom: auto;
  top: 0;
  background: linear-gradient(rgba(0, 0, 0, 0.1) 0%, rgba(0, 0, 0, 0) 100%);
`;
